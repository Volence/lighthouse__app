interface Arguments {
    siteName?: string;
    siteInput: {
        siteName: string;
        mainURL: string;
        categoryURL: string;
        productURL: string;
    };
}

interface Element {}
interface Node {}
interface NodeListOf<TNode = Node> {}
